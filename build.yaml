targets:
  $default:
    builders:
      json_serializable:
        options:
          explicit_to_json: true
          include_if_null: false
          checked: true
      retrofit_generator:
        options:
          nullable: true
      drift_dev:
        options:
          generate_connect_constructor: true
          sqlite: true